---
title: Release Notes for Pivotal Service Instance Manager
owner: Fleet Management
---

<strong><%= modified_date %></strong>

<%= partial vars.path_to_partials + '/pcf-name-change' %>

##<a id="0-8-0"></a> v0.8.0

**Release Date:** MMMM DD, 2020

### Features

New features and changes in this release:

- Filter search results by clicking on a bar in the bar chart in the Service Consumption dashboard.
- Added in-memory cache, which is disabled by default. For more information, see <a href="installing.html#enable-cache">Enable <%= vars.product_short %> cache</a>.

### Resolved Issues

This release has the following fixes:

- Fixed the "431 Request Header Fields Too Large" error that occurred when targeting more than 4 foundations.
- <%= vars.product_short %> now limits the number of parallel requests allowed to ensure that
<%= vars.product_short %> does not deny service to the Cloud Controller.

## <a id="view"></a> View Release Notes for Another Version

To view the release notes for another product version, select the version from
the dropdown at the top of this page.
